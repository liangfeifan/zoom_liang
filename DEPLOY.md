# 发布说明

这个版本已经按当前仓库地址 `liangfeifan/zoom_liang` 配置好，默认发布地址为：

`https://liangfeifan.github.io/zoom_liang/`

## 发布步骤

1. 将本目录内的全部文件覆盖到 GitHub 仓库根目录。
2. 提交并推送到 `master` 或 `main` 分支。
3. 打开仓库的 **Settings → Pages**。
4. 在 **Build and deployment** 中将 **Source** 选择为 **GitHub Actions**。
5. 等待仓库顶部 **Actions** 页面里的部署任务完成。

## 后续补充个人信息

- 邮箱、Google Scholar、ORCID：编辑 `_config.yml` 中的 `author` 区域。
- 个人照片：把照片放入 `images/`，然后把 `_config.yml` 中的 `avatar` 改为照片文件名。
- 首页文字：编辑 `_pages/about.md`。
- 简历：编辑 `_pages/cv.md`。

目前未提供的联系方式和学术账号都保持为空，不会显示模板的虚假示例链接。
